[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.build]
cmds = [
  "cd ScreenSound.API",
  "dotnet restore",
  "dotnet publish -c Release -o out"
]

[start]
cmd = "dotnet ./ScreenSound.API/out/ScreenSound.API.dll"
